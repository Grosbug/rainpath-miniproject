.claude/settings.local.json
backend/test/*.db

# Dependencies (each package also has its own .gitignore for safety)
node_modules/
**/node_modules/

# Build artifacts
**/dist/
**/coverage/
*.tsbuildinfo
**/*.tsbuildinfo

# Logs
*.log
**/*.log

# OS metadata
.DS_Store
**/.DS_Store

# Local scratch files (working notes — not committed)
modifs.txt
docs/interview-prep.md

# Artefacts task-triage-dispatch (suivi de tâches — ne pas versionner)
/TASKS.md
/.task-triage/
