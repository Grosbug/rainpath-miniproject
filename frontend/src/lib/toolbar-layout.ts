/** Grille titre / séparateur / contenu pour barres d’outils (retour + infos). */
export const TOOLBAR_LEADING_GRID = 'grid grid-cols-[auto_1px_1fr] items-center gap-x-3'

/** Colonne gauche fixe — alignement parcours (retour + barre J+). */
export const TOOLBAR_LEADING_GRID_FIXED = 'grid grid-cols-[5rem_1px_1fr] items-center gap-x-3'

export const TOOLBAR_DIVIDER = 'h-8 w-px bg-border justify-self-center'
