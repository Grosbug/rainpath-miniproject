/** Tailwind class string for React Flow handles, by family (DS §7.3). */
export const handleClass =
  '!h-5 !w-5 rounded-full border-2 bg-surface cursor-pointer ' +
  'transition-shadow hover:ring-4 hover:ring-primary/25 ' +
  'data-[handlestate=connecting]:!bg-primary data-[handlestate=connecting]:ring-4 data-[handlestate=connecting]:ring-primary/30'
